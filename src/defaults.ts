import { MetagenOverrides } from './types.js';

const defaults: MetagenOverrides = {
    title: "Nitlix App",
    description: 'A new pre-packed nitlix app.',
    siteName: 'Nitlix',
    themeColor: '#FFFFFF'
}


export default defaults;